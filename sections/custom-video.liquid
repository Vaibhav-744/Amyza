{% comment %}
  Custom Hero Video Section
{% endcomment %}

<style>
  #shopify-section-{{ section.id }} .hero-video{
    position:relative;
    overflow:hidden;
    color:{{ section.settings.text_color }};
    height:100vh;
    min-height:560px;
    display:flex;
    align-items:flex-end;
    background:{{ section.settings.fallback_bg }};
  }

  #shopify-section-{{ section.id }} .hero-video-bg{
    position:absolute;
    inset:0;
    width:100%;
    height:100%;
    object-fit:cover;
    z-index:0;
  }

  #shopify-section-{{ section.id }} .hero-video-overlay{
    position:absolute;
    inset:0;
    z-index:1;
    background:
      linear-gradient(
        180deg,
        {{ section.settings.overlay_top }} 0%,
        {{ section.settings.overlay_middle }} 50%,
        {{ section.settings.overlay_bottom }} 100%
      ),
      radial-gradient(
        ellipse at 30% 70%,
        {{ section.settings.overlay_glow }},
        transparent 60%
      );
  }

  #shopify-section-{{ section.id }} .hero-video-content{
    position:relative;
    z-index:2;
    max-width:720px;
    margin:0 0 8vh 6vw;
    padding:2rem 0;
  }

  #shopify-section-{{ section.id }} .eyebrow{
    font-size:.7rem;
    text-transform:uppercase;
    letter-spacing:.25em;
    margin-bottom:.9rem;
    font-weight:500;
    color:{{ section.settings.eyebrow_color }};
  }

  #shopify-section-{{ section.id }} .hero-title{
    font-family:var(--font-heading-family);
    font-weight:400;
    font-size:clamp(2.4rem,7vw,5.4rem);
    letter-spacing:-.02em;
    line-height:1;
    margin-bottom:1.2rem;
    color:{{ section.settings.heading_color }};
  }

  #shopify-section-{{ section.id }} .hero-title em{
    color:{{ section.settings.heading_italic_color }};
    font-style:italic;
  }

  #shopify-section-{{ section.id }} .hero-title p{
    margin:0;
  }

  #shopify-section-{{ section.id }} .hero-sub{
    color:{{ section.settings.subheading_color }};
    font-size:1.05rem;
    max-width:460px;
    margin-bottom:1.8rem;
  }

  #shopify-section-{{ section.id }} .hero-ctas{
    display:flex;
    gap:.8rem;
    flex-wrap:wrap;
  }

  #shopify-section-{{ section.id }} .btn{
    display:inline-flex;
    align-items:center;
    justify-content:center;
    padding:.95rem 1.8rem;
    border-radius:4px;
    font-size:.7rem;
    letter-spacing:.24em;
    text-transform:uppercase;
    font-weight:500;
    transition:all .3s ease;
    cursor:pointer;
    border:1px solid transparent;
    line-height:1;
    text-decoration:none;
  }

  #shopify-section-{{ section.id }} .btn-primary{
    background:{{ section.settings.primary_button_bg }};
    color:{{ section.settings.primary_button_text }};
    border-color:{{ section.settings.primary_button_border }};
  }

  #shopify-section-{{ section.id }} .btn-primary:hover{
    background:{{ section.settings.primary_button_hover_bg }};
    border-color:{{ section.settings.primary_button_hover_bg }};
  }

  #shopify-section-{{ section.id }} .btn-secondary{
    background:transparent;
    color:{{ section.settings.secondary_button_text }};
    border-color:{{ section.settings.secondary_button_border }};
  }

  #shopify-section-{{ section.id }} .btn-secondary:hover{
    background:{{ section.settings.secondary_button_hover_bg }};
    color:{{ section.settings.secondary_button_hover_text }};
    border-color:{{ section.settings.secondary_button_hover_bg }};
  }

  #shopify-section-{{ section.id }} .video-mute-btn{
    position:absolute;
    top:1.2rem;
    right:1.2rem;
    z-index:3;
    width:42px;
    height:42px;
    border-radius:4px;
    background:{{ section.settings.mute_button_bg }};
    backdrop-filter:blur(10px);
    -webkit-backdrop-filter:blur(10px);
    border:1px solid {{ section.settings.mute_button_border }};
    color:{{ section.settings.mute_button_icon }};
    cursor:pointer;
    display:flex;
    align-items:center;
    justify-content:center;
    transition:.3s;
  }

  #shopify-section-{{ section.id }} .video-mute-btn:hover{
    background:{{ section.settings.mute_button_hover_bg }};
  }

  #shopify-section-{{ section.id }} .video-mute-btn.muted .icon-unmute{
    display:none;
  }

  #shopify-section-{{ section.id }} .video-mute-btn:not(.muted) .icon-mute{
    display:none;
  }

  #shopify-section-{{ section.id }} .scroll-hint{
    position:absolute;
    bottom:1.5rem;
    left:50%;
    transform:translateX(-50%);
    z-index:3;
    display:flex;
    flex-direction:column;
    align-items:center;
    gap:.5rem;
    color:{{ section.settings.scroll_text_color }};
    opacity:.8;
    display:none;
  }

  #shopify-section-{{ section.id }} .scroll-hint span{
    font-size:.62rem;
    letter-spacing:.3em;
    text-transform:uppercase;
  }

  #shopify-section-{{ section.id }} .scroll-line{
    width:1px;
    height:40px;
    background:{{ section.settings.scroll_line_color }};
    animation:heroScrollPulse {{ section.settings.scroll_animation_speed }}s ease-in-out infinite;
    transform-origin:top;
  }

  @keyframes heroScrollPulse{
    0%,100%{
      transform:scaleY(.3);
      opacity:.4;
    }
    50%{
      transform:scaleY(1);
      opacity:1;
    }
  }

  @media screen and (max-width:980px){

    #shopify-section-{{ section.id }} .hero-video{
      min-height:500px;
    }

    #shopify-section-{{ section.id }} .hero-video-content{
      margin:0 0 20vh;
      padding:0 1.5rem;
    }

    #shopify-section-{{ section.id }} .hero-title{
      font-size:clamp(2.2rem,10vw,3.4rem);
    }

    #shopify-section-{{ section.id }} .hero-sub{
      font-size:.92rem;
      margin-bottom:1.2rem;
    }

    #shopify-section-{{ section.id }} .video-mute-btn{
      top:.8rem;
      right:.8rem;
      width:38px;
      height:38px;
    }
  }
</style>

<section class="hero-video" id="hero-{{ section.id }}">

  {% if section.settings.video != blank %}
    {{
      section.settings.video
      | video_tag:
        image_size: '2000x',
        autoplay: true,
        loop: true,
        controls: false,
        playsinline: true,
        preload: 'auto',
        class: 'hero-video-bg'
    }}
  {% endif %}

  <div class="hero-video-overlay"></div>

  <div class="hero-video-content">

    {% if section.settings.eyebrow != blank %}
      <p class="eyebrow">
        {% if settings.shape != blank %}
        <span class="shape">{{ settings.shape }}</span>
        {% endif %}
        {{ section.settings.eyebrow }}
      </p>
    {% endif %}

    {% if section.settings.heading != blank %}
      <div class="hero-title">
        {{ section.settings.heading }}
      </div>
    {% endif %}

    {% if section.settings.subheading != blank %}
      <p class="hero-sub">
        {{ section.settings.subheading }}
      </p>
    {% endif %}

    <div class="hero-ctas">

      {% if section.settings.primary_button_label != blank %}
        <a
          href="{{ section.settings.primary_button_link }}"
          class="btn btn-primary"
        >
          {{ section.settings.primary_button_label }}
        </a>
      {% endif %}

      {% if section.settings.secondary_button_label != blank %}
        <a
          href="{{ section.settings.secondary_button_link }}"
          class="btn btn-secondary"
        >
          {{ section.settings.secondary_button_label }}
        </a>
      {% endif %}

    </div>
  </div>

  {% if section.settings.show_mute_button %}
    <button
      class="video-mute-btn muted"
      type="button"
      aria-label="Toggle sound"
    >

      <svg class="icon-mute" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M11 5 6 9H2v6h4l5 4V5z"/>
        <line x1="23" y1="9" x2="17" y2="15"/>
        <line x1="17" y1="9" x2="23" y2="15"/>
      </svg>

      <svg class="icon-unmute" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M11 5 6 9H2v6h4l5 4V5z"/>
        <path d="M15 9.5a5 5 0 0 1 0 5"/>
        <path d="M18.5 7a9 9 0 0 1 0 10"/>
      </svg>

    </button>
  {% endif %}

  {% if section.settings.show_scroll_hint %}
    <div class="scroll-hint">
      <span>{{ section.settings.scroll_text }}</span>
      <div class="scroll-line"></div>
    </div>
  {% endif %}

</section>

<script>
  (() => {

    const section = document.querySelector('#shopify-section-{{ section.id }}');

    if (!section) return;

    const video = section.querySelector('video');
    const muteButton = section.querySelector('.video-mute-btn');

    if (!video || !muteButton) return;

    video.muted = true;
    video.volume = 1;

    muteButton.addEventListener('click', async () => {

      try {

        if (video.muted) {

          video.muted = false;
          video.removeAttribute('muted');

          video.volume = 1;

          await video.play();

          muteButton.classList.remove('muted');

        } else {

          video.muted = true;
          video.setAttribute('muted', 'muted');

          muteButton.classList.add('muted');

        }

      } catch (error) {
        console.log(error);
      }

    });

  })();
</script>

{% schema %}
{
  "name": "Custom Hero Video",
  "tag": "section",
  "class": "section-hero-video",
  "settings": [
    {
      "type": "video",
      "id": "video",
      "label": "Background Video"
    },
    {
      "type": "color",
      "id": "fallback_bg",
      "label": "Fallback Background",
      "default": "#731157"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "✦ Introducing the Golden Nectar"
    },
    {
      "type": "color",
      "id": "eyebrow_color",
      "label": "Eyebrow Color",
      "default": "#EAACC9"
    },
    {
      "type": "richtext",
      "id": "heading",
      "label": "Heading",
      "default": "<p><em>The Glow</em><br>You Deserve.</p>"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#F1EDEC"
    },
    {
      "type": "color",
      "id": "heading_italic_color",
      "label": "Italic Color",
      "default": "#EAACC9"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "A slow-luxury ritual rooted in ancestral botanical wisdom — reimagined for the modern woman."
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Subheading Color",
      "default": "#F1EDEC"
    },

    {
      "type": "header",
      "content": "Primary Button"
    },
    {
      "type": "text",
      "id": "primary_button_label",
      "label": "Label",
      "default": "Shop The Serum"
    },
    {
      "type": "url",
      "id": "primary_button_link",
      "label": "Link"
    },
    {
      "type": "color",
      "id": "primary_button_bg",
      "label": "Background",
      "default": "#731157"
    },
    {
      "type": "color",
      "id": "primary_button_text",
      "label": "Text",
      "default": "#F1EDEC"
    },
    {
      "type": "color",
      "id": "primary_button_border",
      "label": "Border",
      "default": "#731157"
    },
    {
      "type": "color",
      "id": "primary_button_hover_bg",
      "label": "Hover Background",
      "default": "#4E0A3A"
    },

    {
      "type": "header",
      "content": "Secondary Button"
    },
    {
      "type": "text",
      "id": "secondary_button_label",
      "label": "Label",
      "default": "Discover The Ritual →"
    },
    {
      "type": "url",
      "id": "secondary_button_link",
      "label": "Link"
    },
    {
      "type": "color",
      "id": "secondary_button_text",
      "label": "Text",
      "default": "#F1EDEC"
    },
    {
      "type": "color",
      "id": "secondary_button_border",
      "label": "Border",
      "default": "#F1EDEC"
    },
    {
      "type": "color",
      "id": "secondary_button_hover_bg",
      "label": "Hover Background",
      "default": "#F1EDEC"
    },
    {
      "type": "color",
      "id": "secondary_button_hover_text",
      "label": "Hover Text",
      "default": "#731157"
    },

    {
      "type": "header",
      "content": "Overlay"
    },
    {
      "type": "color_background",
      "id": "overlay_top",
      "label": "Top Overlay",
      "default": "rgba(42,10,32,0.25)"
    },
    {
      "type": "color_background",
      "id": "overlay_middle",
      "label": "Middle Overlay",
      "default": "rgba(42,10,32,0.5)"
    },
    {
      "type": "color_background",
      "id": "overlay_bottom",
      "label": "Bottom Overlay",
      "default": "rgba(42,10,32,0.85)"
    },
    {
      "type": "color_background",
      "id": "overlay_glow",
      "label": "Glow Overlay",
      "default": "rgba(115,17,87,0.35)"
    },

    {
      "type": "header",
      "content": "Mute Button"
    },
    {
      "type": "checkbox",
      "id": "show_mute_button",
      "label": "Show Button",
      "default": true
    },
    {
      "type": "color_background",
      "id": "mute_button_bg",
      "label": "Background",
      "default": "rgba(42,10,32,0.45)"
    },
    {
      "type": "color",
      "id": "mute_button_border",
      "label": "Border",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "mute_button_icon",
      "label": "Icon",
      "default": "#F1EDEC"
    },
    {
      "type": "color_background",
      "id": "mute_button_hover_bg",
      "label": "Hover Background",
      "default": "rgba(42,10,32,0.7)"
    },

    {
      "type": "header",
      "content": "Scroll Hint"
    },
    {
      "type": "checkbox",
      "id": "show_scroll_hint",
      "label": "Show Scroll Hint",
      "default": true
    },
    {
      "type": "text",
      "id": "scroll_text",
      "label": "Text",
      "default": "Scroll"
    },
    {
      "type": "color",
      "id": "scroll_text_color",
      "label": "Text Color",
      "default": "#F1EDEC"
    },
    {
      "type": "color",
      "id": "scroll_line_color",
      "label": "Line Color",
      "default": "#F1EDEC"
    },
    {
      "type": "range",
      "id": "scroll_animation_speed",
      "label": "Animation Speed",
      "min": 1,
      "max": 5,
      "step": 0.5,
      "unit": "s",
      "default": 2
    },

    {
      "type": "color",
      "id": "text_color",
      "label": "Base Text Color",
      "default": "#F1EDEC"
    }
  ],
  "presets": [
    {
      "name": "Custom Hero Video"
    }
  ]
}
{% endschema %}